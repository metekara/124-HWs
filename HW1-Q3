#include <stdio.h>
#include <math.h>

int main( void )
{
	double popularity,size;
	printf("Enter the populartiy:\n");
	scanf("%lf",&popularity);
	printf("Enter the size:\n");
	scanf("%lf",&size);
	printf("Homevalue is %.f",(pow(popularity,3) + pow(size,2)) * 10000);
	return 0;
} 
